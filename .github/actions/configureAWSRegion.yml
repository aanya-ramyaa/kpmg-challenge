name: "Configure AWS Region"
description: "Receives AWS Region and output it"
inputs:
  region:
    description: 'AWS Region to Deploy in'
    required: true
    default: 'ap-south-1'
outputs:
  stack-id: # id of output
    description: "AWS Region for deployment"
